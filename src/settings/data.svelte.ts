const cssSettings = $state({
    outerContainer: "",
    groupContainer: "",
    groupHeader: "",
    rollContainer: "",
});

type CssSettings = typeof cssSettings;

export { cssSettings };
export type { CssSettings };
